fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
pydantic-settings==2.3.4
email-validator==2.2.0
python-multipart==0.0.9
python-dotenv==1.0.1
httpx==0.28.1

# AI + vector store — installed separately since they're heavy; the app boots
# and runs without them, falling back to demo-mode answers (see
# app/services/gemini_service.py and app/services/embeddings_service.py)
chromadb==0.5.0
google-genai==0.3.0
